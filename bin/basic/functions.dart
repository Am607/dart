// functions or method
//big codes are classified into small groups called function
// function is a set of group
// use is code reuse and easy to make
// void is a data type main is function name {cerly brakent is body }
// baraket afer the function is used for passing the parameter
// local variable used onlu in that function
/*void main(List<String> args) {
  String nameFromFunction = printName();
  print(nameFromFunction);
 

  String name = "acer laptop";
  String newname = printNames(name, 10);
  print(newname);
  String book = "biology";
  int a = 10;
  printValue(book, a);
}
// impotant very
String printName() {
  String name = "Akbar masterpadi";
  return name;
  print('..........................');
}

String printNames(String name, int counter) {
  String newName = name + ".....";
  return newName;
}
// another example

void printValue(String book, int counter1) {
  for (int i = 0; i < counter1; i = i + 1) {
    print(book);
  }
}
*/
void main() {
  aFunction(); // calling of  a function into main function
  bFunction();
}

void aFunction() {
// body
  String name = "Akbarmaasterpadi";
  print(name);
}

String bFunction() {
// body
  String name;

  name = "asd";
  print(name);
  return name; // or also give value directly here
  // now we are retune the variable
}
/*
// dart pad examble 
here a function value is dirclty called to main function
void main(){
   String valueFromAFunction = aFunction();
  print(valueFromAFunction);
}


String aFunction(){
  String name ="biology";
 // print(name);
  return name;
}

*/
//.,.,.,,.,.,.,.,.,.,..,.,.,.,.,.,.,.,.,.,.,...,..,.,,
/*
void main(){
  String name = "biology";
  String newName = aFunction(name);
  print(newName);
  
}

String aFunction( String name){
  String newName= name + "akbar";
  return newName;
   
}
*/

// one answer in two type of code ane in function and other in for loop
/*



*/
