// class is a big group can group fuctions and datas
// next level grouping
// use is code advanced reduce;;
// class name first letter is in capital letter
// function name is in camelcase

void main() {
  AClassName amal = AClassName();
  //this is type like string amal object last one is class name
  amal.aFunction();
  AClassName akku = AClassName();
  print(akku.b);
  // here akku is object so here for akku dart created
  // all parameters of class for akku
  // this is called oop in dart
  // key word name is not used for as a variable
}

class AClassName {
  String a = "hello world";
  int b = 20;
  void aFunction() {
    print(a);
  }
}
// consructor is a function name exactly as class
// and no return type
// after creating an object and give value into class then constructor
// will exicute(Function) and assign in to parametres on the class

//_--------------------------------------------------------------------

//   this is key word and used to denote object
// in the case of +> object value pass in to class
// parameters =>pass value to => constructro=> assign value into => class variable
